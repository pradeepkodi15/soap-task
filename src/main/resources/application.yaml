camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Kermit
  filter:
    create: All tasks
server:
   servlet:
    context-path: /Camunda
    
camunda:
    bpm:
        database:
            type: postgres
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: root
        url: jdbc:postgresql://localhost:5432/spring-camunda
        username: postgres